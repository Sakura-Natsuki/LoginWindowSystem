<Window x:Class="LoginWindowSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:LoginWindowSystem.ViewModels"
        xmlns:helpers="clr-namespace:LoginWindowSystem.Helpers"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginWindowSystem.Views"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:LoginViewModel,
                                         IsDesignTimeCreatable=True}"
        Title="CYBERPUNK 2077 // 身份验证" 
        Height="480" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <helpers:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <helpers:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>
    
    <Border CornerRadius="6" Background="#0D1117"
            BorderBrush="#F7E018" BorderThickness="1.5">
        <Grid Margin="30,25">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                
                <RowDefinition Height="36"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="36"/>
                <RowDefinition Height="auto"/>
                
                <RowDefinition Height="auto"/>
                <RowDefinition Height="52"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="// 接口登录 //" Foreground="#F7E018"
                       FontSize="26" FontWeight="Bold" FontFamily="Consolas"
                       HorizontalAlignment="Center" Margin="0,6,0,4">  
                
            </TextBlock>
        </Grid>
    </Border>
</Window>
