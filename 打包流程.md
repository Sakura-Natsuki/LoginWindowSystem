
## 整体架构（联机 / 集中式数据库）

本项目采用**客户端-服务器**架构，SQL Server 集中部署在一台服务器上，所有客户端通过网络连接同一个数据库：

```
┌──────────┐   ┌──────────┐   ┌──────────┐
│ 客户端 A  │   │ 客户端 B  │   │ 客户端 C  │   ← 仅安装 WPF 应用，无需 SQL Server
└────┬─────┘   └────┬─────┘   └────┬─────┘
     │               │               │
     └───────────────┼───────────────┘
           TCP 1433 / 内网或公网
                     │
              ┌──────┴──────┐
              │  SQL Server │   ← 一台服务器，集中部署
              │  数据库引擎   │      运行 InitDatabase() 一次即可
              └─────────────┘
```

**客户端零数据库依赖**——用户电脑只需要 .NET Framework 4.8 运行环境（Win10 1903+ / Win11 已内置），无需安装 SQL Server 或任何数据库组件。

---

## ⚠️ 发布前的必要修改

在打包之前，必须完成以下修改。当前代码**硬编码了本地连接字符串**，不适用于联机场景。

### 🔴 必须修改：连接字符串

**现状**（`DatabaseService.cs:14`）：

```csharp
private const string CONN_STR =
    "Data Source=.;Initial Catalog=CyberpunkLoginDB;Integrated Security=True;";
```

三个问题：

| 问题 | 说明 |
|------|------|
| `Data Source=.` | 指向本机，联机场景应指向服务器 IP 或域名 |
| `Integrated Security=True` | Windows 集成身份验证——仅适用于同一 Windows 域的局域网环境。跨网络/公网场景应使用 SQL Server 身份验证 |
| `const` 硬编码 | 服务器地址变更需要重新编译，应改为从 `App.config` 读取 |

**修改方案：**

**第一步**：修复 `App.config` 中的连接字符串（已修复笔误 `Ture` → `True`）：

```xml
<connectionStrings>
    <add name="CyberpunkDB"
         connectionString="Data Source=你的服务器IP或域名;
                           Initial Catalog=CyberpunkLoginDB;
                           User ID=你的数据库用户名;
                           Password=你的数据库密码;"
         providerName="System.Data.SqlClient"/>
</connectionStrings>
```

认证方式选择：

| 场景 | 连接字符串关键字 | 说明 |
|------|-----------------|------|
| **内网 + Windows 域** | `Integrated Security=True;` | 无需用户名密码，使用客户端当前登录的域账户 |
| **内网 / 公网（推荐）** | `User ID=xxx;Password=xxx;` | SQL Server 身份验证，需在服务器上创建 SQL 登录账户 |
| **Azure SQL / 云数据库** | 同上 + `Encrypt=True;` | 云端 SQL Server，强制加密连接 |

**第二步**：修改 `DatabaseService.cs`，从 `App.config` 读取连接字符串（而非 `const` 硬编码）：

```csharp
// 替换原来的 const CONN_STR
private static string GetConnectionString()
{
    var connStr = System.Configuration.ConfigurationManager
        .ConnectionStrings["CyberpunkDB"]?.ConnectionString;
    
    if (string.IsNullOrEmpty(connStr))
        throw new InvalidOperationException("未在 App.config 中找到连接字符串 CyberpunkDB");
    
    return connStr;
}
```

> 需要添加引用：右键项目 → 添加引用 → 程序集 → 勾选 `System.Configuration`

### 🟡 建议修改

| 问题 | 位置 | 说明 |
|------|------|------|
| `App.config` 笔误 | `App.config:9` | `Integrated Security=Ture;` → 已修复为 `True`（如果是 SQL 身份验证则此项不出现） |
| 缺少应用图标 | — | 未定义 `.ico` 图标文件，安装后在开始菜单显示默认图标 |
| 程序集元数据为空 | `AssemblyInfo.cs` | `AssemblyTitle`、`AssemblyCompany`、`AssemblyDescription` 均为空字符串 |

---

## 服务器端：一次性部署 SQL Server

客户端分发之前，需要先有一台可访问的 SQL Server 并建好库表。有三种方式获取：

| 方式 | 适用场景 | 成本 |
|------|---------|------|
| **自建服务器** | 内网环境，已有 Windows Server | 硬件 + SQL Server 许可证（Express 版免费） |
| **Azure SQL Database** | 公网访问，不想管服务器运维 | 按量付费，约 $5/月起 |
| **云服务器自建** | 阿里云/腾讯云等，安装 SQL Server Express | 云服务器费用，SQL Server Express 免费 |

部署后只需运行一次应用（或手动执行 `InitDatabase()` 中的建表 SQL），`Users` 表和初始 `admin` 账户即创建完毕。

---

## 推荐打包方式

针对本项目（WPF / .NET Framework 4.8 / VS / 联机架构 / 客户端分发 / 版本管理），推荐以下方式：

### 🥇 首选：ClickOnce

**为什么选它：**
- Visual Studio 内置，无需额外工具
- 自动版本管理——发布新版本后，用户打开应用自动提示更新
- 安装到用户目录，**不需要管理员权限**
- 可发布到 Web 服务器、网络共享或本地文件夹
- 控制面板可正常卸载

**适合场景：** 频繁更新、需要自动升级的内网/公网客户端分发。

---

### 🥈 备选：MSI 安装包（VS Installer Projects 扩展）

**为什么选它：**
- 标准 `.msi` 安装包，用户体验最熟悉
- 系统级安装到 `Program Files`，所有 PC 用户账户均可用
- 可创建桌面快捷方式、开始菜单条目、文件关联
- 支持通过组策略 (GPO) / SCCM 在企业内推送

**适合场景：** 对外公开发布、企业批量部署。

---

## 打包流程

### 方式一：ClickOnce

#### 1. 发布步骤（Visual Studio）

1. 确保项目在 **Release** 配置下编译通过
2. **解决方案资源管理器** → 右键项目 → **发布**
3. 选择发布位置：
   - **Web 服务器**（推荐）—— 发布到 IIS / 云存储，用户通过 URL 在线安装，自动更新最方便
   - **网络共享** —— `\\server\share\AppName`，适合纯内网
   - **文件夹** —— 发布到本地目录，手动分发给用户
4. 设置安装 URL（与发布位置相同或指向分发的地址）
5. 点击 **发布**

#### 2. 版本管理

- 每次发布 VS 自动递增版本号（项目属性 → 发布 → 发布版本）
- 勾选"应用程序应检查更新" → 选择"在应用程序启动前"
- 用户下次启动时自动检测新版本并升级，无需任何手动操作
- 可在项目属性中设置"最低要求版本"强制用户升级

#### 3. 分发给用户的方式

| 方式 | 用户操作 |
|------|---------|
| Web 发布 | 将 `publish.htm` 的 URL 发给用户，浏览器中点击安装 |
| 网络共享 | 将共享路径发给用户，运行其中的 `setup.exe` |
| 文件夹 | 将发布文件夹打包发给用户，运行 `setup.exe` |

#### 4. 关于 `App.config` 在 ClickOnce 中的处理

发布后，`App.config` 会被重命名为 `LoginWindowSystem.exe.config` 并放在应用目录中。**如果将来需要修改连接字符串**，用户可直接编辑该文件，或通过 ClickOnce 的配置文件 API 在代码中更新。

> 建议：在应用启动时加一个连接失败重试逻辑，并在界面上给用户提供修改服务器地址的入口（通过设置窗口）。

---

### 方式二：MSI 安装包（VS Installer Projects 扩展）

#### 1. 安装扩展

Visual Studio → **扩展** → **管理扩展** → 搜索：
> **Microsoft Visual Studio Installer Projects 2022**

#### 2. 添加安装项目

1. **文件** → **添加** → **新建项目** → 搜索 **Setup Project**
2. 命名为 `LoginWindowSystem.Setup`

#### 3. 配置安装项目

1. 右键 Setup 项目 → **Add** → **项目输出** → **主输出** → 确定（自动包含 .exe + .dll）
2. 右键 Setup 项目 → **Add** → **文件** → 添加 `App.config`（发布后为 `LoginWindowSystem.exe.config`）
3. 属性面板中设置：
   - **Author** / **Manufacturer**：你的名字/公司名
   - **ProductName**：Cyberpunk 登录系统
   - **Version**：与 `AssemblyInfo.cs` 保持一致
4. 右键 Setup 项目 → **视图** → **文件系统** → 创建桌面和开始菜单快捷方式

#### 4. 前置条件

右键 Setup 项目 → **属性** → **前置条件**：
- ✅ **.NET Framework 4.8**
- ❌ **不需要** SQL Server（客户端不装数据库）

#### 5. 生成

**Release** 配置 → 右键 Setup 项目 → **生成** → 输出 `.msi` 文件。

---

## 发布前操作清单

1. [x] 将 `DatabaseService.cs` 的连接字符串从 `const` 改为从 `App.config` 读取
2. [x] 在 `App.config` 中填写正确的服务器地址、用户名、密码
3. [x] 添加对 `System.Configuration` 的引用
4. [x] 确保 SQL Server 服务器已部署并可被客户端访问（防火墙开放 1433 端口）
5. [x] 在服务器上运行一次应用以创建数据库和初始数据（或手动执行建表 SQL）
6. [x] 添加应用图标（`.ico`），在项目属性 → 应用程序中设置
7. [x] 填写 `AssemblyInfo.cs` 中的 Company、Description 等元数据
8. [x] Release 配置编译通过
9. [x] 在一台干净机器上测试安装运行（或虚拟机）
10. [x] 选择打包方式并执行发布

---

## 补充说明

- **.NET Framework 4.8**：Win10 1903+ 和 Win11 已内置，客户端无需额外安装
- **版本号规范**：`主版本.次版本.生成号.修订号`。ClickOnce 会自动递增发布版本
- **代码签名**：分发到公网建议对 ClickOnce 清单或 MSI 进行代码签名，避免 SmartScreen "未知发布者" 警告
- **SQL Server 端口**：默认 1433，确保服务器防火墙放行。公网部署强烈建议使用非默认端口 + IP 白名单
- **App.config 存放位置**：编译后自动复制为 `LoginWindowSystem.exe.config`，与 `.exe` 同级目录，打包时无需特殊处理
- **连接失败处理**：建议在 `App.xaml.cs` 的 `OnStartup` 中捕获 SQL 连接异常，给出友好提示而非直接崩溃
